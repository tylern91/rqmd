# Git LFS — binary / large-file tracking.
# Any file matching these patterns is stored in LFS rather than the main repo object store.
# -text disables line-ending normalization (binaries must not be touched).
#
# New binary files (models, images, archives) will be tracked automatically when added;
# no manual `git lfs track` step is required.

*.png     filter=lfs diff=lfs merge=lfs -text
*.jpg     filter=lfs diff=lfs merge=lfs -text
*.jpeg    filter=lfs diff=lfs merge=lfs -text
*.gif     filter=lfs diff=lfs merge=lfs -text
*.webp    filter=lfs diff=lfs merge=lfs -text
*.pdf     filter=lfs diff=lfs merge=lfs -text
*.gguf    filter=lfs diff=lfs merge=lfs -text
*.onnx    filter=lfs diff=lfs merge=lfs -text

# *.tar.gz / *.zip / *.bin intentionally NOT LFS-tracked: release tarballs are built
# in the CI runner workspace and never committed, so tracking them here is a pure
# footgun — one accidental commit of a 6-9 MB tarball goes straight into LFS against
# the 1 GB free quota. Re-add only if a real binary asset in one of these extensions
# needs to be committed to the repo.
